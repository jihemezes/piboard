{
  "id": "webview",
  "version": "0.4.0",
  "name": {
    "en": "Web page",
    "fr": "Page web"
  },
  "description": {
    "en": "Embeds any web page (traffic map, home app, status page…). Most sites work out of the box, even ones that normally block being shown in an iframe.",
    "fr": "Affiche n'importe quelle page web (carte de trafic, app domotique, page de statut…). Fonctionne d'emblée avec la plupart des sites, y compris ceux qui bloquent normalement leur affichage en iframe."
  },
  "tagline": {
    "en": "Displays any web page inside a tile.",
    "fr": "Affiche n'importe quelle page web dans une tuile."
  },
  "size": {
    "w": 6,
    "h": 4,
    "minW": 2,
    "minH": 2,
    "maxW": 12,
    "maxH": 16
  },
  "settings": [
    {
      "key": "url",
      "type": "text",
      "default": "",
      "label": {
        "en": "Page URL",
        "fr": "URL de la page"
      },
      "hint": {
        "en": "\"https://\" is added automatically if left out.",
        "fr": "« https:// » est ajouté automatiquement si vous ne le tapez pas."
      }
    },
    {
      "key": "mode",
      "type": "select",
      "default": "proxy",
      "label": {
        "en": "Display mode",
        "fr": "Mode d'affichage"
      },
      "hint": {
        "en": "In the Windows desktop app, the site is always shown directly and interactively (X-Frame-Options is ignored there) — these modes then only matter if you pick \"Image\". On a Raspberry Pi in kiosk mode: \"Via PiBoard\" relays the page from the server (works for sites blocking iframe embedding); \"Direct\" loads the address as-is (fastest, but only if the site allows embedding); \"Image\" renders the page with a real browser and shows a photo (works with absolutely any site, but static and a few seconds per refresh).",
        "fr": "Dans l'application de bureau Windows, le site est toujours affiché directement et reste interactif (X-Frame-Options y est ignoré) — ces modes n'ont alors d'effet que si vous choisissez « Image ». Sur un Raspberry Pi en mode kiosque : « Via PiBoard » relaie la page depuis le serveur (fonctionne pour les sites bloquant l'affichage en iframe) ; « Direct » charge l'adresse telle quelle (le plus rapide, mais seulement si le site l'autorise) ; « Image » fait rendre la page par un vrai navigateur et affiche une photo (fonctionne avec absolument n'importe quel site, mais affichage fixe et quelques secondes par rafraîchissement)."
      },
      "options": [
        {
          "value": "proxy",
          "label": {
            "en": "Via PiBoard (works around most blocked sites)",
            "fr": "Via PiBoard (contourne la plupart des sites bloqués)"
          }
        },
        {
          "value": "direct",
          "label": {
            "en": "Direct (faster, site must allow embedding)",
            "fr": "Direct (plus rapide, le site doit autoriser l'affichage)"
          }
        },
        {
          "value": "shot",
          "label": {
            "en": "Image (works with any site, not interactive)",
            "fr": "Image (fonctionne avec n'importe quel site, non interactif)"
          }
        }
      ]
    },
    {
      "key": "zoom",
      "type": "number",
      "default": 100,
      "min": 25,
      "max": 200,
      "step": 5,
      "label": {
        "en": "Zoom (%)",
        "fr": "Zoom (%)"
      }
    },
    {
      "key": "reload",
      "type": "number",
      "default": 0,
      "min": 0,
      "max": 1440,
      "step": 1,
      "label": {
        "en": "Reload every (minutes, 0 = never)",
        "fr": "Recharger toutes les (minutes, 0 = jamais)"
      },
      "hint": {
        "en": "In \"Image\" mode, this is also how often the page is re-photographed. A short interval means relaunching a browser often on the PiBoard — 10 minutes or more is usually plenty.",
        "fr": "En mode « Image », c'est aussi la fréquence à laquelle la page est re-photographiée. Un intervalle court signifie relancer souvent un navigateur sur le PiBoard — 10 minutes ou plus suffisent généralement."
      }
    }
  ]
}
